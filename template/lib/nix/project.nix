# Tier 3 — Project-specific Nix module
# This file is yours to customize. Copier will not overwrite it.
# Use lib.mkAfter to extend base.nix devPkgs:
#   { lib, pkgs, ... }: { devPkgs = lib.mkAfter [ pkgs.nodejs ]; }
{ ... }: { }
